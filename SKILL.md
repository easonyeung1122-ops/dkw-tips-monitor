---
name: dkw-tips-monitor
agent_created: true
description: >
  美债通胀补偿高频监测。把美联储 DKW 分解（D'Amico-Kim-Wei 2018 JFQA，Kim-Walsh-Wei 2019 FEDS Note）
  的官方 CSV 与 Bloomberg 日频市场数据结合，产出日度简报：市场侧零模型分解（名义 = 实际 + 盈亏平衡）、
  Fed 口径三成分归因（ΔIC = Δ预期通胀 + Δ通胀风险溢价 − ΔTIPS流动性溢价 + Δ残差）、TIPS 相对模型的
  贵贱残差、以及带样本外误差带的桥接外推，把 Fed 序列约 2–3 周的发布滞后补到最新交易日。
  每次运行另出**一段不带术语的大白话解读**（报告第 0 节 + stdout）。
  触发：TIPS、通胀补偿、盈亏平衡通胀、breakeven、盈亏平衡、DKW、通胀预期分解、通胀风险溢价、
  流动性溢价、TIPS 贵贱、5y5y、5-to-10-year IC、UST inflation compensation、通胀补偿监测、
  美债通胀定价、DKW updates。
---

# UST 通胀补偿高频监测（DKW / TIPS）

把美联储官方 DKW 分解与 Bloomberg 实盘数据拼成一条**日频**流水线，回答一个问题：

> 通胀补偿（IC）今天的变动，里面有多少是通胀预期，有多少是风险溢价/流动性，多少是模型解释不了的？

## 何时使用

- 用户要求看/跟踪美债盈亏平衡通胀、TIPS 通胀补偿、通胀预期分解。
- 用户问"今天盈亏平衡动了多少、是预期还是溢价驱动""TIPS 贵还是便宜"。
- 用户提到 DKW、D'Amico-Kim-Wei、Kim-Walsh-Wei、`DKW_updates.csv`。
- 用户要一份美债通胀定价的日度/每周简报。

**不要用于**：FOMC 政策利率路径预测（那是 `fed-watch`）、名义期限溢价的 ACM 分解、TIPS 具体券的做市报价。

## 工作流

```
fetch_fed_csv → load_dkw ─┐
                          ├→ run_bridge → render → UST_IC_<date>.md
fetch_bbg ───────────────┘
```

一条命令跑完（路径用 Windows 风格，见"环境"）：

```bash
SW="$HOME/.workbuddy/skills/dkw-tips-monitor"   # 改成你的 skill 实际目录
PY="${DKW_PY:-$(command -v python3 || command -v py || command -v python)}"
"$PY" "$SW/scripts/run.py" --outdir "$HOME/dkw_out"
```

从任意工作目录调用都可以（脚本自己处理 `sys.path`）。首次运行会自动下载两个数据源
（Fed CSV ≈ 4.6MB，Bloomberg 面板 ≈ 1.1MB）。

### run.py 参数

| 参数 | 说明 |
|---|---|
| `--asof YYYY-MM-DD` | 复现历史某日截面；默认取最新可用交易日 |
| `--outdir PATH` | 报告落盘目录，默认 `<skill>/reports`。**必须写 Windows 路径** |
| `--refresh-fed` | 强制重下 Fed CSV（缓存默认 7 天） |
| `--no-bbg` | 跳过 Bloomberg，只出 Fed 侧（无终端时自动降级） |
| `--refresh-bbg` | 强制全量重拉 Bloomberg 历史 |
| `--window N` | 滚动估计窗口，默认 750 个交易日 |
| `--terse` | 只打一行结论 |

## 报告结构（第 0 节 + 七节，可靠性递减分层）

| 节 | 内容 | 可靠性 |
|---|---|---|
| **0** | **大白话版**：一段不带术语的结论解读，每次运行必出（同时也打到 stdout） | 派生自下方各节数值，**不引入新数据** |
| 1 | 市场侧分解（两张表）：**1.1 水平快照**（5y/10y/20y/30y/5y5y/10y10y/20y10y 的名义、实际、盈亏平衡、口径差）+ **1.2 变动矩阵**（行 = 度量 × 窗口，列 = 期限） | **零模型观测**，最可靠 |
| 2 | Fed DKW 分解：三期限 × 五成分 + IC + z 分 | 权威，但落后 2–3 周 |
| 3 | 归因：`ΔIC(市场) = Δ预期通胀 + ΔIRP − ΔTIPS流动性溢价 + Δ残差` | Fed 覆盖期内**精确可加** |
| 4 | **4.1** 残差（Fed 口径，滞后 2–3 周） + **4.2 实时残差**（推到最新交易日，自算） | 4.1 权威；4.2 是唯一**带 z 分、可直接用**的输出 |
| 5 | 桥接外推至最新交易日 + 样本外误差带 | **自算估计**，必须如此标注 |
| 6 | 情绪联动：IC 周变动与 HY OAS / VIX / Brent 的相关性（复现原 Note Table 1 构造） | 观测 |
| 7 | 数据溯源与限制 | — |

### 第 0 节：大白话版（`analyze.plain_read()`）

**每次运行必须产出**，位置在结论行之后、所有表格之前。作用：不懂 term premium / z 分的人也能读懂并据此行动。

叙述顺序固定为决策顺序，**不跳步**：

1. **时间与幅度** —— Fed 最后发布日到今天的交易日数，10y 通胀补偿从多少变成多少、上下行几个 bp。
2. **谁在推动** —— 用 1.2 的近 1 月（缺则近 1 周）10y 数据把名义拆成实际利率 / 通胀两块，报出实际利率占比。
3. **通胀预期本身动了吗** —— 直接用 `classify()` 的判定标签换成人话（残差主导 → "读不出来"；预期驱动 → "确实动了"；溢价驱动 → "不是对通胀的看法变了"）。
4. **有没有错价** —— 4.2 的 `z_live` 与 `d_bp vs band_bp` 换成"几倍平常波动"和"是不是这几天新冒出来的"，并给出方向性动作。
5. **一句话总结**。

**约束**：

- 它是**纯派生函数**——只用下面各节已经算好的数字，不得自行取数或另立口径。表格与它冲突时以表格为准，那是 bug。
- 全程避免术语，或在括号里就地翻译（"实际利率——借真钱、扣掉通胀后的成本"）。唯一保留的量化语言（z 值、基点）在段尾统一给白话对照。
- 数字格式：日期一律 `YYYY-MM-DD`（走 `_d()`，别直接插 Timestamp，会打出 `2026-08-31 00:00:00`）；带符号量走 `_num()`，避免 `-0.0`。
- 措辞改动改 `plain_read()`，**不要**手改生成的 `.md`——下次运行会被覆盖。
- `render()` 返回 `(path, plain_paragraph)`；`run.py` 用它打印到 stdout，`--terse` 只抑制打印、不影响报告内的第 0 节。

### 排版约定（别退回成"每个窗口一张表"）

一张表里混装**随窗口变**和**不随窗口变**的量，会让同一列在多张表里逐字重复，读者会以为表格出错了。
规则：

- **不随窗口变的量**（水平、口径差、z 分）→ **只出现一次**。
- **随窗口变的量**（Δ）→ 窗口变成**行**，期限是**列**（一张变动矩阵），不要每个窗口复制一张表。

第 1 节就是按这条拆成 1.1 水平快照 + 1.2 变动矩阵。修改时若新增窗口，只加行不加表。

## 怎么用（决策路径）

报告是**分解原料**，不是信号机。**只看一段**就先读第 0 节（大白话版），它已经把下面四个问题的答案串成人话；
要核对或自己重算，再按顺序翻表：

| 想问 | 看哪 | 判据 |
|---|---|---|
| 名义利率这波是通胀还是实际利率在动？ | **1.1 + 1.2** | `Δ实际/Δ名义`。>80% → 实际利率主导，跟通胀预期无关；这一步**零模型、实时**，不用等 Fed |
| 通胀**预期**本身动了吗？ | **1.2 的 Δ盈亏平衡行 → 第 3 节** | 1.2 只给幅度；第 3 节把同一变动拆成预期/溢价/流动性/残差，取**绝对值最大**的那项当定性 |
| 有没有可交易的错价？ | **4.2 实时残差** | `z_live` 判**水平**显著性（跨年尺度），`\|d_bp\| vs band_bp` 判**近期变化**显著性（短期）。**两个都要过** |
| 这几天实际发生了什么？ | **5 节 + 6 节** | 5 节给 Fed 没覆盖那段的成分外推；6 节判风险因子是否异常（>0.5σ 则该周 IC 变动倾向溢价渠道） |

**顺序不能反**：先 1.2 看 IC 有没有动，再翻第 3 节找原因。跳过 1.2 直接读第 3 节会拿着 2–3 周前的归因当今天的行情。

**三个最常犯的错**

1. **把盈亏平衡的变动读成通胀预期。** 只有在第 3 节里"预期通胀"是最大单项时才能这么读。残差主导 → 那笔变动落在 DKW 三成分之外（TIPS 供需/未建模流动性），什么预期都没说。
2. **拿 4.1 的 z 直接下单。** 4.1 的锚点是 Fed 最后发布日，常落后 2–3 周。要用必须先看 4.2 的 `d_bp` 有没有把偏离走掉。
3. **把"水平显著"当"刚出现"。** `z_live` 高只说明这个偏离相对 5 年分布不小；`|d_bp| < band_bp` 说明它**不是这几天新发生的**，追进去要承担它继续走阔的可能。

## 方法论要点

**恒等式（已在全样本 11,391 个交易日验证，最大误差 6e-14）**

```
nominal.yield.fitted = exp.real.short.rate + exp.inflation + real.term.prem + inflation.risk.prem
ic.fitted            = exp.inflation + inflation.risk.prem − tips.liq.prem
```

**归因的精确性**：`Δic.raw = Δic.fitted + Δ残差`，而 `Δic.fitted` 就是三成分之和，所以第 3 节四项严格加总等于市场 IC 的变动。这是本工具最强的一块。

**桥接外推**：滚动岭回归拟合**日变动**并锚定 Fed 最后水平。

```
d(component)_t = a + b'·dX_t + e_t        X = Δ实际收益率、Δ盈亏平衡、ΔVIX、ΔHY OAS、Δ油价、ΔMOVE
component_t    = anchor + γ · Σ_{s≤t} d(component)_s     anchor = Fed 最后发布的水平
```

- γ 是**整条 h 日路径的阻尼系数**（h = 实际需桥接的交易日数），在较早切片上拟合，`γ=1` 不阻尼、`γ→0` 表示累积后无方向性内容。
- λ 与 γ 都在**较早的 400 日切片**上选，指标在**之后未用于选参的 400 日**上计算。基准是随机漫步。
- **误差带必须取 `band95_h_bp`**（验证期实测 h 日误差的 95% 分位），**不要**用 `1.96 × 1 日 RMSE × √h` —— 误差高度持续，√h 会把带宽低估近一个数量级。

**信号判定**：比较各渠道的**绝对值大小**，不是占净变动的比例（净变动接近 0 时占比会爆掉）。残差为最大单项 → "模型残差主导"；预期通胀为最大单项 → "预期驱动"；溢价渠道为最大项 → "情绪/溢价驱动"（若同时风险因子周变动 > 0.5σ 更明确）。

## 环境（Windows 注意事项）

```bash
# Python 解释器：默认按 python3 → py(Windows 启动器) → python 顺序探测。
# 若你的 `python` 指向 Microsoft Store 占位程序，设 DKW_PY 指向真实解释器即可：
#   export DKW_PY="C:/Path/To/Python312/python.exe"
PY="${DKW_PY:-$(command -v python3 || command -v py || command -v python)}"
```

- **Python 需装好 `blpapi 3.26.5.1` + `pandas` + `numpy` + `scipy`**。Windows 上若 `python` 命令被 Microsoft Store 占位程序劫持，
  用 `py` 启动器（`py scripts/run.py`）或设置 `DKW_PY` 环境变量指向真实解释器全路径；不要在脚本里用裸 `python`。
- Bloomberg 需要 `bbcomm.exe` 在跑（终端已登录）。会话起不来就加 `--no-bbg`。
- **`--outdir` 与脚本路径都建议传 Windows 风格路径**（`C:/...`）。若用 git-bash 的 `/c/...` 形式，
  会被 Python 解释成当前盘根（`C:\c\Users\...` 孤儿目录 / `No such file or directory`）。两条都实际踩过。

### 单模块自检

```bash
"$PY" "$SW/scripts/dkw_fetch.py"    # 下载 + 解析 + 恒等式校验 + 打印 vintage 说明
"$PY" "$SW/scripts/bbg_fetch.py"    # 拉取并缓存 Bloomberg 面板
"$PY" "$SW/scripts/bridge.py"       # 只看桥接外推的样本外指标
```

## 已知坑（都已处理，改代码时别退回）

| 坑 | 处理 |
|---|---|
| Fed CSV 的真实 URL 是**下划线** `DKW_updates.csv` | Note 正文里写的连字符版本 `DKW-updates.csv` 是死链，别用 |
| TIPS generic 收益率**零填充** | 5y 是 `USGGT05YR`，`USGGT5YR` 无效 |
| 无 15y 名义 generic | 15y 直接跳过（Fed 模型内部用了 15y，但市场侧拿不到） |
| 无 5y5y 远期 ticker | 用 5y/10y 两点复利自算，`bbg_fetch.forward()` |
| 油价用 **Brent**（`CO1 Comdty`）不是 WTI | 前月 WTI 在 2020-04-20 收于 −37.63，取对数变 NaN 并污染回归 |
| Fed 序列在**全部工作日**发布（含美国假日） | Bloomberg 面板要过滤到 Mon–Fri 再 `ffill(limit=4)`，否则日历网格对不齐 |
| 单条 BDH 请求多券会分多个 message | 必须循环处理 `PARTIAL_RESPONSE`，否则丢一半 ticker |
| h 日指标公式 | `R²_h = 1 − E[err_model²]/E[err_rw²]`，`err_h` **已经是误差**，不要再减一次实际值 |
| 空档为 0 时 | 结论行会显示"源间基差"而不是"变动"，别当成市场移动 |
| `{:+.1f}` 会把 −0.0001 渲染成 `-0.0`，看着像 bug | `_md_table` 内统一走 `_neg_zero()` 改成 `+0.0` |
| 展示量必须与它的"变化性"匹配 | 不随窗口变的列（水平、口径差）只列一次；随窗口变的做成变动矩阵。见"排版约定" |
| 第 1 节可自校验 | `Δ名义 = Δ实际 + Δ盈亏平衡` 因为 `实际 ≡ 名义 − 盈亏平衡` 而**逐行恒等**，最大残差 3.6e-15bp；改代码后顺手一验 |
| 彭博 IC 与 Fed `ic.raw` **不是同一个测量** | 水平基差均值 +2.3bp、sd 4.5bp(10y)/11.6bp(5y5y)，与信号同量级 → 残差**不能按水平**跨源相减，必须走变动口径（见 4.2） |
| `resid_real` 是 Fed 自己的会计口径 | = `resid_nom − resid_ic`，**不是** TII ticker 减 Fed 拟合实际收益率（那样算出来差 40bp+） |
| 残差的误差带要**方差相加** | `resid_real` 是两条桥接序列之差 → `band = √(band_nom² + band_ic²)`。只用一条会把带宽低估约 30% |
| 第 0 节（大白话版）必须每次运行都出 | 它是 `plain_read()` 的纯派生输出，不取新数、不改口径；表格与它冲突时以表格为准。措辞只在 `plain_read()` 里改 |
| 第 0 节插日期/数字不能裸用对象 | 日期走 `_d()`（否则打出 `2026-08-31 00:00:00`），带符号量走 `_num()`（否则 `-0.0`） |

## 仓库文档

`README.md` 是仓库（GitHub）的前台介绍：解决的问题、报告结构、快速开始、三项核心技术、读法与限制。
**改方法或改输出结构时同步更新它**——它是唯一一份写给"没用过这个工具的人"看的文档，
SKILL.md 面向的是执行者（措辞更紧、含踩坑清单），两者受众不同，不要合并。

README 中的表格若在单元格里写含竖线的表达式（如 `|d_bp|`），**必须转义成 `\|`**，
否则 GitHub 会把表格切错列。SKILL.md 的第 113 行踩过这个坑。

## 数据源

| 源 | 内容 | 频率 |
|---|---|---|
| `federalreserve.gov/econres/notes/feds-notes/DKW_updates.csv` | DKW 分解，27 列 × 3 期限，1983-01-03 起 | 月度发布，日度序列 |
| 同上页面 | 当前 vintage 的模型设定说明（再估样本截止日等） | 随 CSV 更新 |
| Bloomberg（blpapi） | 名义/实际收益率、盈亏平衡、VIX/HY OAS/MOVE/Brent/通胀互换 | 日频，实时 |

**发布节奏**：每月第 4 个工作日 10:00 后更新，序列前推至上月最后工作日 → 常年落后最新交易日约 2–3 周。
**非官方统计发布**，美联储明示可无预告延迟、修订或变更方法。

**注意**：当前 CSV 的 vintage 与 2019 年 FEDS Note **方法论已不同**——改用 BCFF 月度 1 年通胀预测
（原为 SPF 季度），并加入 15y、20y 名义与 TIPS 收益率参与估计，模型参数基于固定再估样本外推。
引用时务必说明所用 vintage，`dkw_fetch.vintage_notes()` 会返回原文说明行。

## 依赖

`pandas` / `numpy`（需 Python 3.12+）、`blpapi`（Bloomberg）。无 `tabulate` 依赖
（表格渲染自带）。网络通过 `urllib`（stdlib）取 Fed CSV。
